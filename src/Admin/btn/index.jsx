import React from 'react'
import styles from './index.module.css';
const Btn = () => {
    return (
        <button type='submit' className={styles.btn}>
            Add User
        </button>
    )
}

export default Btn