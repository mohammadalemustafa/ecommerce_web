import React from "react";
import styles from "./index.module.css";

const MiniSectionTitle = ({ title }) => {
  return <h4 className={styles.h4}>{title}</h4>;
};

export default MiniSectionTitle;
